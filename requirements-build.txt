# Direct dependencies used by the Windows release build.
# Update these deliberately, then rerun --max, source E2E, and frozen EXE smoke.
-r requirements-dev.txt

edge-tts==7.2.8
matplotlib==3.11.0
aiohttp==3.14.1
ruff==0.15.22
coverage==7.15.2
pyinstaller==6.21.0
